//! A node that removes a directory and all its contents.

/// Remove a directory and all its contents.
fn @[pulsar_node_fn_id]@() -> Result<(), String> {
    match std::fs::remove_dir_all(@[in_path_string]@) {
        Ok(()) => Ok(()),
        Err(e) => Err(format!("Failed to remove directory: {}", e)),
    }
}