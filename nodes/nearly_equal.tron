//! A pure function that checks if two numbers are nearly equal within a tolerance.

/// Check if two numbers are nearly equal within a tolerance.
fn @[pulsar_node_fn_id]@() -> bool {
    let a = @[in_a_number]@;
    let b = @[in_b_number]@;
    let tolerance = @[in_tolerance_number]@;
    (a - b).abs() <= tolerance
}