//! A pure function that applies cubic ease-out interpolation.

/// Apply cubic ease-out interpolation (very slow end).
fn @[pulsar_node_fn_id]@() -> f32 {
    let t = @[in_t_number]@.clamp(0.0, 1.0);
    let f = t - 1.0;
    1.0 + f * f * f
}