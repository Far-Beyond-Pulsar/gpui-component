//! A pure function that selects between two booleans based on a condition (ternary operator).

/// Select between two booleans based on a boolean condition.
fn @[pulsar_node_fn_id]@() -> bool {
    if @[in_condition_bool]@ {
        @[in_a_bool]@
    } else {
        @[in_b_bool]@
    }
}