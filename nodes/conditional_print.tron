//! A debug node that prints only if a condition is true.

/// Print a message only if the condition is true.
fn @[pulsar_node_fn_id]@() {
    if @[in_condition_bool]@ {
        println!("[CONDITIONAL] {}", @[in_message_string]@);
    }
}