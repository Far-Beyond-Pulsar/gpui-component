/// Routing Component
/// Track routing and sends

use super::state::*;
use super::panel::DawPanel;
use gpui::*;

pub fn render_routing(_state: &DawUiState, _cx: &mut Context<DawPanel>) -> impl IntoElement {
    div()
}
