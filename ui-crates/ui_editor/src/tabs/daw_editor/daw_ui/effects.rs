/// Effects Component
/// Effect chain and parameters

use super::state::*;
use super::panel::DawPanel;
use gpui::*;

pub fn render_effect_slot(_state: &DawUiState, _cx: &mut Context<DawPanel>) -> impl IntoElement {
    div()
}
