/// Automation Component
/// Automation curve editing

use super::state::*;
use super::panel::DawPanel;
use gpui::*;

pub fn render_automation_lane(_state: &DawUiState, _cx: &mut Context<DawPanel>) -> impl IntoElement {
    div()
}
